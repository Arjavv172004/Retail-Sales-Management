{
  "name": "truestate-retail-sales-system",
  "version": "1.0.0",
  "description": "Retail Sales Management System - TruEstate SDE Intern Assignment",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run kill-port && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "kill-port": "lsof -ti:5000 | xargs kill -9 2>/dev/null || true"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

